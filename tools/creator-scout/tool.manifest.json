{
  "name": "creator-scout",
  "version": "1.2.0",
  "description": "Search CreatorScout for exact comparable-game creators and inspect billing while explicitly separating authentication, validated targets, prepared checkout, and confirmed subscription states.",
  "entry": "index.js",
  "input": ["text/plain", "application/json"],
  "output": ["application/json", "text/plain"],
  "category": "marketing",
  "keywords": ["creators", "youtube", "twitch", "influencers", "email", "outreach", "games", "contacts", "exact-match", "provenance", "billing", "checkout", "stripe"],
  "configSchema": {
    "EMAIL": {
      "type": "string",
      "required": true,
      "secret": true,
      "prompt": "CreatorScout account email address."
    }
  }
}
