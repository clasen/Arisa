{
  "name": "x-dm",
  "version": "0.4.0",
  "description": "Safely search X/Twitter with persistent campaign query history, accumulate a chat-scoped pool of public prospects, and manage explicitly confirmed outreach, follows, profile updates, and posts.",
  "entry": "index.js",
  "toolDependencies": {
    "browser-session-bridge": "^0.1.0"
  },
  "input": [
    "text/plain",
    "application/json"
  ],
  "output": [
    "text/plain",
    "application/json"
  ],
  "configSchema": {
    "X_COOKIES": {
      "type": "string",
      "required": false,
      "secret": true,
      "prompt": "Optional fallback X/Twitter cookies as a JSON array, Netscape cookies.txt, or raw Cookie header. The tool first tries the chat's browser-session-bridge x.com session, then this value, then x-session-reader cookies."
    },
    "X_CHAT_PASSCODE": {
      "type": "string",
      "required": false,
      "secret": true,
      "prompt": "Optional X Chat passcode used to unlock encrypted conversation history in the persistent chat-scoped browser session."
    },
    "CHROME_EXECUTABLE_PATH": {
      "type": "string",
      "required": false,
      "prompt": "Optional Chrome/Chromium executable path. Defaults to Playwright's cached Chromium."
    },
    "HEADLESS": {
      "type": "string",
      "required": false,
      "prompt": "Run browser headless: true or false."
    },
    "BROWSER_CACHE_MAX_MB": {
      "type": "string",
      "required": false,
      "prompt": "Maximum discardable Chromium cache usage in MiB for the persistent X profile; cookies, local storage, and IndexedDB are preserved."
    },
    "MIN_SECONDS_BETWEEN_SENDS": {
      "type": "string",
      "required": false,
      "prompt": "Minimum seconds between actual DM sends."
    },
    "EXPECTED_ACCOUNT_HANDLE": {
      "type": "string",
      "required": false,
      "prompt": "Optional X handle guard. Sending is blocked if a different logged-in handle is detected."
    },
    "MAX_SENDS_PER_DAY": {
      "type": "string",
      "required": false,
      "prompt": "Maximum confirmed X DMs per UTC day across all campaigns."
    },
    "MIN_SECONDS_BETWEEN_FOLLOWS": {
      "type": "string",
      "required": false,
      "prompt": "Minimum seconds between verified follow actions."
    },
    "MAX_FOLLOWS_PER_DAY": {
      "type": "string",
      "required": false,
      "prompt": "Maximum tool-created follows per UTC day."
    },
    "OPERATION_TIMEOUT_MS": {
      "type": "string",
      "required": false,
      "prompt": "Maximum browser operation time in milliseconds, clamped between 30000 and 180000."
    },
    "MAX_SENDS_PER_CAMPAIGN_PER_DAY": {
      "type": "string",
      "required": false,
      "prompt": "Maximum confirmed X DMs per UTC day for one campaign."
    },
    "MIN_SECONDS_BETWEEN_REPLIES": {
      "type": "string",
      "required": false,
      "prompt": "Minimum seconds between verified public X replies."
    },
    "MAX_REPLIES_PER_DAY": {
      "type": "string",
      "required": false,
      "prompt": "Maximum verified public X replies per UTC day."
    }
  },
  "skillHints": [],
  "category": "social",
  "keywords": [
    "x",
    "twitter",
    "dm",
    "message",
    "outreach",
    "campaign",
    "prospects",
    "candidate-pool",
    "query-history",
    "discovery",
    "playwright",
    "cookies",
    "social",
    "profile",
    "bio",
    "post"
  ]
}
