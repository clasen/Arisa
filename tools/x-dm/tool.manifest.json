{
  "name": "x-dm",
  "version": "0.2.0",
  "description": "Safely manage X/Twitter outreach, follows, the logged-in account bio, and explicitly confirmed posts with account pinning and post-change verification.",
  "entry": "index.js",
  "input": [
    "text/plain",
    "application/json"
  ],
  "output": [
    "text/plain",
    "application/json"
  ],
  "configSchema": {
    "X_COOKIES": {
      "type": "string",
      "required": false,
      "secret": true,
      "prompt": "Paste your X/Twitter cookies as a JSON array, Netscape cookies.txt, or raw Cookie header. If empty, the tool tries the chat's x-session-reader cookies."
    },
    "X_CHAT_PASSCODE": {
      "type": "string",
      "required": false,
      "secret": true,
      "prompt": "Optional X Chat passcode used to unlock encrypted conversation history in the persistent chat-scoped browser session."
    },
    "CHROME_EXECUTABLE_PATH": {
      "type": "string",
      "required": false,
      "prompt": "Optional Chrome/Chromium executable path. Defaults to Playwright's cached Chromium."
    },
    "HEADLESS": {
      "type": "string",
      "required": false,
      "prompt": "Run browser headless: true or false."
    },
    "MIN_SECONDS_BETWEEN_SENDS": {
      "type": "string",
      "required": false,
      "prompt": "Minimum seconds between actual DM sends."
    },
    "EXPECTED_ACCOUNT_HANDLE": {
      "type": "string",
      "required": false,
      "prompt": "Optional X handle guard. Sending is blocked if a different logged-in handle is detected."
    },
    "MAX_SENDS_PER_DAY": {
      "type": "string",
      "required": false,
      "prompt": "Maximum confirmed X DMs per UTC day across all campaigns."
    },
    "MIN_SECONDS_BETWEEN_FOLLOWS": {
      "type": "string",
      "required": false,
      "prompt": "Minimum seconds between verified follow actions."
    },
    "MAX_FOLLOWS_PER_DAY": {
      "type": "string",
      "required": false,
      "prompt": "Maximum tool-created follows per UTC day."
    },
    "OPERATION_TIMEOUT_MS": {
      "type": "string",
      "required": false,
      "prompt": "Maximum browser operation time in milliseconds, clamped between 30000 and 180000."
    },
    "MAX_SENDS_PER_CAMPAIGN_PER_DAY": {
      "type": "string",
      "required": false,
      "prompt": "Maximum confirmed X DMs per UTC day for one campaign."
    },
    "MIN_SECONDS_BETWEEN_REPLIES": {
      "type": "string",
      "required": false,
      "prompt": "Minimum seconds between verified public X replies."
    },
    "MAX_REPLIES_PER_DAY": {
      "type": "string",
      "required": false,
      "prompt": "Maximum verified public X replies per UTC day."
    }
  },
  "skillHints": [],
  "category": "social",
  "keywords": [
    "x",
    "twitter",
    "dm",
    "message",
    "outreach",
    "campaign",
    "playwright",
    "cookies",
    "social",
    "profile",
    "bio",
    "post"
  ]
}
