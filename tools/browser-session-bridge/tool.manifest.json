{
  "name": "browser-session-bridge",
  "version": "0.1.3",
  "description": "Connect browser profiles and receive explicitly shared, domain-scoped sessions while keeping profile connection, session sharing, and target validation as separate states.",
  "entry": "index.js",
  "toolDependencies": {
    "lightpanda-browser": "^0.11.1"
  },
  "daemon": {
    "scope": "global",
    "autoStart": true,
    "health": "internal"
  },
  "input": ["text/plain", "application/json", "application/zip"],
  "output": ["application/json", "application/zip"],
  "category": "web",
  "keywords": ["browser", "chrome", "brave", "cookies", "extension", "session", "playwright", "lightpanda", "authentication"],
  "configSchema": {
    "PUBLIC_BASE_URL": {
      "type": "string",
      "required": true,
      "prompt": "Public HTTP(S) base URL where the browser extension can reach the bridge, for example https://bridge.example.com."
    },
    "CHROME_WEB_STORE_URL": {
      "type": "string",
      "required": false,
      "prompt": "Published Chrome Web Store URL for one-click extension installation."
    },
    "PAIR_TTL_SECONDS": {
      "type": "number",
      "required": false,
      "prompt": "Lifetime of a one-time pairing code in seconds."
    }
  }
}
