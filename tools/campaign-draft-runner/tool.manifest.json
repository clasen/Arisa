{
  "name": "campaign-draft-runner",
  "version": "0.1.0",
  "toolDependencies": { "pr-campaign": "^0.1.0", "gmail-workspace": "^0.1.0" },
  "description": "Run configurable outreach draft batches from chat-scoped profiles. Discovers public editorial contacts, keeps coverage and contact provenance separate, validates grounded personalization and language, and creates Gmail drafts only; never sends mail.",
  "entry": "index.js",
  "input": ["text/plain", "application/json"],
  "output": ["application/json", "text/plain"],
  "category": "communication",
  "keywords": ["campaign", "contacts", "discovery", "drafts", "gmail", "polling", "outreach", "pr", "research", "personalization", "provenance", "preflight", "configurable"],
  "configSchema": {
    "DEFAULT_LIMIT": { "type": "number", "required": false, "prompt": "Default number of drafts per batch." },
    "DEFAULT_PROFILE": { "type": "string", "required": false, "prompt": "Default profile name." }
  },
  "skillHints": [
    { "name": "stop-slop", "when": "drafting personalized outreach emails" }
  ]
}
