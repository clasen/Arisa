{
  "name": "campaign-draft-runner",
  "version": "0.3.2",
  "toolDependencies": { "pr-campaign": "^0.1.0", "gmail-workspace": "^0.1.0" },
  "description": "Run configurable outreach draft batches from chat-scoped profiles. Measures search quality, skips exhausted sources for 30 days, adapts reviewer-first discovery, maintains owner-approved product facts, validates grounded provenance and language, and creates Gmail drafts only; never sends mail.",
  "entry": "index.js",
  "input": ["text/plain", "application/json"],
  "output": ["application/json", "text/plain"],
  "category": "communication",
  "keywords": ["campaign", "contacts", "discovery", "drafts", "gmail", "metrics", "outreach", "pr", "quality", "facts", "research", "source-exhaustion", "personalization", "provenance", "preflight", "configurable"],
  "configSchema": {
    "DEFAULT_LIMIT": { "type": "number", "required": false, "prompt": "Default number of drafts per batch." },
    "DEFAULT_PROFILE": { "type": "string", "required": false, "prompt": "Default profile name." }
  },
  "skillHints": [
    { "name": "stop-slop", "when": "drafting personalized outreach emails" }
  ]
}
