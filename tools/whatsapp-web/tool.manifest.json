{
  "name": "whatsapp-web",
  "description": "Use isolated per-chat WhatsApp Web sessions to log in, send messages, and receive/process WhatsApp replies via whatsapp-web.js. Recommendation: use a dedicated auxiliary number because WhatsApp Web automation can risk Meta restrictions or bans.",
  "entry": "index.js",
  "daemon": {
    "scope": "chat",
    "autoStart": false,
    "health": "internal"
  },
  "input": [
    "text/plain",
    "image/*",
    "video/*",
    "audio/*",
    "application/pdf"
  ],
  "output": [
    "text/plain",
    "application/json"
  ],
  "configSchema": {
    "PHONE_NUMBER": {
      "type": "string",
      "required": false,
      "prompt": "Optional phone number for WhatsApp pairing-code login, including country code."
    },
    "DEFAULT_COUNTRY_CODE": {
      "type": "string",
      "required": false,
      "prompt": "Optional default country code used when normalizing local phone numbers."
    },
    "HEADLESS": {
      "type": "string",
      "required": false,
      "prompt": "Run the WhatsApp browser session headless: true or false."
    },
    "CHROME_EXECUTABLE_PATH": {
      "type": "string",
      "required": false,
      "prompt": "Optional path to Chrome/Chromium executable for WhatsApp Web."
    },
    "WEB_VERSION": {
      "type": "string",
      "required": false,
      "prompt": "Optional WhatsApp Web version override. Leave empty to use the current library default."
    },
    "WEB_VERSION_CACHE_STRICT": {
      "type": "string",
      "required": false,
      "prompt": "Whether to require the cached WhatsApp Web bundle for WEB_VERSION: true or false."
    },
    "IDLE_SHUTDOWN_MS": {
      "type": "string",
      "required": false,
      "prompt": "Milliseconds to keep an unwatched per-chat WhatsApp session live before closing the browser and reconnecting on demand."
    },
    "HUMANIZE_SEND": {
      "type": "string",
      "required": false,
      "prompt": "Whether send mode should wait/show typing before sending by default: true or false."
    },
    "HUMANIZE_SEND_INITIAL_DELAY_MS": {
      "type": "string",
      "required": false,
      "prompt": "Milliseconds to wait before showing typing and sending a WhatsApp message."
    },
    "HUMANIZE_SEND_ONLINE_LEAD_MS": {
      "type": "string",
      "required": false,
      "prompt": "Milliseconds to appear online before WhatsApp typing starts."
    },
    "HUMANIZE_SEND_POST_SEND_ONLINE_MS": {
      "type": "string",
      "required": false,
      "prompt": "Milliseconds to remain online after sending before becoming unavailable."
    },
    "HUMANIZE_SEND_TYPING_MS": {
      "type": "string",
      "required": false,
      "prompt": "Milliseconds to show WhatsApp typing before sending a message."
    },
    "HUMANIZE_SEND_MAX_MS": {
      "type": "string",
      "required": false,
      "prompt": "Maximum allowed milliseconds for humanized send delays."
    }
  },
  "category": "communication",
  "keywords": [
    "contacts",
    "daemon",
    "essential",
    "inbox",
    "messaging",
    "send",
    "watch",
    "whatsapp"
  ]
}
