{
  "name": "real-video-analyzer",
  "description": "Prepare a video for agent interpretation with scene-aware keyframes, visual deduplication, transcript extraction, and a manifest using the claude-real-video approach.",
  "entry": "index.js",
  "input": ["video/*", "text/plain"],
  "output": ["application/zip"],
  "configSchema": {
    "PYTHON_COMMAND": { "type": "string", "required": false, "prompt": "Optional Python command. Defaults to python3." },
    "CRV_COMMAND": { "type": "string", "required": false, "prompt": "Optional claude-real-video command. Defaults to the bundled installation." },
    "MAX_FRAMES": { "type": "number", "required": false, "prompt": "Maximum scene-aware keyframes to extract." },
    "WHISPER_MODEL": { "type": "string", "required": false, "prompt": "Optional Whisper model when local Whisper support is installed." }
  },
  "category": "media",
  "keywords": ["video", "analysis", "keyframes", "scene-detection", "transcript", "vision", "claude-real-video"],
  "skillHints": []
}
