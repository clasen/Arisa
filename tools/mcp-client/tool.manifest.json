{
  "name": "mcp-client",
  "version": "0.2.0",
  "description": "Connect to allowlisted remote MCP servers, dynamically expose every discovered tool, normalize MCP results, materialize typed media, and manage OAuth with chat-scoped profiles.",
  "entry": "index.js",
  "input": ["text/plain", "application/json", "image/*", "application/pdf", "application/octet-stream"],
  "output": ["application/json", "text/plain", "image/*", "audio/*", "video/*", "application/octet-stream"],
  "category": "integration",
  "keywords": ["audio", "client", "dynamic-tools", "image", "integration", "mcp", "model-context-protocol", "oauth", "remote-tools", "streamable-http", "video"],
  "configSchema": {
    "REQUEST_TIMEOUT_MS": { "type": "number", "required": false, "prompt": "Maximum duration of one MCP request in milliseconds." },
    "MAX_RESULT_BYTES": { "type": "number", "required": false, "prompt": "Maximum accepted MCP result size in bytes." },
    "ALLOW_PRIVATE_HOSTS": { "type": "boolean", "required": false, "prompt": "Allow private or loopback MCP server addresses. Keep false unless explicitly required." }
  }
}
